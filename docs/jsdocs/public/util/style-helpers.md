## Functions

<dl>
<dt><a href="#getDarkenedBg">getDarkenedBg(color)</a> ⇒ <code>String</code></dt>
<dd><p>Given a color, generates a slightly darkened color to be used
as a background color. See the StyledCard component for an example.</p>
</dd>
<dt><a href="#getDarkenedBorder">getDarkenedBorder(color)</a> ⇒ <code>String</code></dt>
<dd><p>Given a color, generates a slightly darkened color to be used
as a border color. See the StyledCard component for an example.</p>
</dd>
<dt><a href="#getInverseLuminance">getInverseLuminance(color)</a> ⇒ <code>String</code></dt>
<dd><p>Given a color, returns white or black based on the luminance
value of the input color.</p>
</dd>
</dl>

<a name="getDarkenedBg"></a>

## getDarkenedBg(color) ⇒ <code>String</code>
Given a color, generates a slightly darkened color to be used
as a background color. See the StyledCard component for an example.

**Kind**: global function  

| Param | Type |
| --- | --- |
| color | <code>String</code> | 

<a name="getDarkenedBorder"></a>

## getDarkenedBorder(color) ⇒ <code>String</code>
Given a color, generates a slightly darkened color to be used
as a border color. See the StyledCard component for an example.

**Kind**: global function  

| Param | Type |
| --- | --- |
| color | <code>String</code> | 

<a name="getInverseLuminance"></a>

## getInverseLuminance(color) ⇒ <code>String</code>
Given a color, returns white or black based on the luminance
value of the input color.

**Kind**: global function  

| Param | Type |
| --- | --- |
| color | <code>String</code> | 

