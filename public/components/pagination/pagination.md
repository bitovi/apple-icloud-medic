@parent medic
@module {can.Component} @public/components/pagination <Pagination>

Generic Pagination component

@signature `<Pagination limit={this.viewModel.limit} offset={this.viewModel.offset} total={this.viewModel.length}`

@body

## Use

