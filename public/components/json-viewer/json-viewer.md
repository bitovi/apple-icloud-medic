@parent medic
@module {can.Component} medic/public/components/json-viewer <JSONViewer />

Renders a nicely formatted, interactable JSON tree.

@signature `<ReactJson src={this.viewModel.src} theme={this.viewModel.theme} displayDataTypes={this.viewModel.displayDataTypes} collapsed={this.viewModel.collapsed} />`

@body

## Use

