import React from 'react';
import ReactDOM from 'react-dom';
import AppComponent from './app/';
import './util/canjs-extensions';

ReactDOM.render(<AppComponent />, document.getElementById('application'));
